export const meta = {
  name: 'geez-fix-critics',
  description: 'Resolve the 12-critic issues: correctness, mobile perf, loop, UX, culture, a11y, security.',
  phases: [{ title: 'Fix', detail: '6 parallel agents, distinct files' }],
}

const RESULT = {
  type: 'object',
  additionalProperties: false,
  required: ['file', 'status', 'notes'],
  properties: {
    file: { type: 'string' },
    status: { type: 'string', enum: ['done', 'partial', 'blocked'] },
    notes: { type: 'string' },
  },
}

const PROJECT =
  'Project: C:\\Users\\mike-work\\Desktop\\geez-art — a Vite+TS, vanilla-DOM, zero-backend web app ("geez·art") turning photos/videos into Ge\'ez fidel mosaics. Strict tsconfig (noUnusedLocals/Parameters). EDIT ONLY YOUR ASSIGNED FILE(S). Do NOT touch render.ts (already fixed), tests/e2e.spec.ts, or any other agent\'s file. After editing, run `npx tsc --noEmit` from the root and REPORT whether it passes (other agents may be mid-edit — report rather than block).'

phase('Fix')
const results = await parallel([
  () => agent(
    PROJECT + '\n\nASSIGNED: src/input.ts.\nThe reviewers found TWO real bugs. Fix both:\n1) EXIF DOUBLE-ROTATION: decodeRaw() currently falls back to createImageBitmap(blob) and loadViaImg(<img>) — both apply the browser\'s default EXIF auto-rotation — then orientAndComposite() applies the manual EXIF transform on top. Change decodeRaw to return `{ src: Decoded; rotated: boolean }` where `rotated` is TRUE for the fallback paths (browser already applied EXIF) and FALSE for the imageOrientation:\'none\' path. In imageFileToCanvas, when `rotated` is true, pass orientation as undefined/1 to orientAndComposite (skip the manual transform); otherwise pass the exifr-detected orientation. Update all call sites. Also fix the now-stale top-of-file comment that claims double-rotation "cannot occur".\n2) SOURCE DOWNSCALING (the biggest mobile-perf win): orientAndComposite currently keeps natural camera resolution (4000×3000). Downscale the OUTPUT canvas so the LONG edge is capped at 1600px (preserve aspect; skip upscaling if smaller). This cuts the per-render getImageData + luminance loop ~10-25× on phone photos. Verify tsc.',
    { label: 'fix:input', phase: 'Fix', schema: RESULT }
  ),
  () => agent(
    PROJECT + '\n\nASSIGNED: src/video.ts.\nFixes for the video frame loop (reviewers: "resizes srcCanvas every tick, reallocates the canvas each frame even when dimensions are unchanged" and "samples full-res frames on low-end phones"):\n1) In startVideoLoop, only set srcCanvas.width/height when they actually differ from the current values (compare before assigning) so the canvas isn\'t cleared/reallocated every frame.\n2) Downscale the video frame: cap the srcCanvas long edge at 1600px (preserve aspect) so the per-frame render doesn\'t sample full 1080p/4K pixels. (e.g. compute a scale = min(1, 1600 / max(video.videoWidth, video.videoHeight)) and size the canvas accordingly, drawing via drawImage with the scale — simplest is to draw the video into a canvas whose dims are the capped ones: ctx.drawImage(video, 0, 0, capW, capH).)\nKeep the rest of video.ts (recordCanvas with audio param, recordGIF, the {blob, ext} return shape) unchanged. Verify tsc.',
    { label: 'fix:video', phase: 'Fix', schema: RESULT }
  ),
  () => agent(
    PROJECT + '\n\nASSIGNED: src/export.ts.\nTwo fixes:\n1) "Save as HTML" must be genuinely self-contained. exportHTML currently emits a <pre> with a system font stack — on macOS/iOS there is no default Ethiopic font, so recipients see tofu. Fix: make exportHTML async; fetch the Noto Ethiopic woff2 (import its URL: `import ethiopicWoffUrl from \'@fontsource-variable/noto-sans-ethiopic/files/noto-sans-ethiopic-ethiopic-wght-normal.woff2\';` — Vite emits the asset), read it as base64 (`const b64 = await (await fetch(ethiopicWoffUrl)).arrayBuffer()` then convert), and embed a `@font-face { font-family:"Fidel"; src: url("data:font/woff2;base64,...") }` inside a <style> in the exported document; use font-family:"Fidel" on the <pre>. Update app.ts\'s caller? NO — you cannot touch app.ts. Instead, export exportHTML async and ALSO export a small sync wrapper if needed, and note in `notes` that the caller in app.ts must now `await exportHTML(...)`. Actually — app.ts calls `exportHTML(res.chars, {...})` in downloadHTML. Since you cannot touch app.ts, make the change backward-compatible: keep `exportHTML` as is BUT add a new `export `selfContainedHTML(chars, opts): Promise<string>` that does the font embedding, and leave exportHTML for backward-compat. Verify tsc (importing a .woff2 URL needs a declaration — add `declare module \'*.woff2\'` in a small `src/assets.d.ts`).\n2) BRAND BAND OVERLAP at low column counts: in makeShareImage, the CTA "make yours at <url>" can collide with the wordmark on narrow mosaics (cols=20 → ~280px wide). Make the band layout adapt: measure the wordmark + CTA widths and if the CTA would collide (wordmark-right + gap > url-left), reduce the CTA font size until it fits, or drop the "make yours at " prefix and show only the URL. Keep the viral CTA when space allows. Verify tsc.',
    { label: 'fix:export', phase: 'Fix', schema: RESULT }
  ),
  () => agent(
    PROJECT + '\n\nASSIGNED: src/app.ts.\nFix ALL of the following (reviewers found these):\n1) VIDEO AUDIO IS SILENT: the source <video> is muted (v.muted=true), so captured audio is silence. In the #dlVideo click handler, before capturing audio: temporarily set videoEl.muted = false; videoEl.volume = 0; then capture via `videoEl.captureStream()` (the cast already exists), then restore videoEl.muted = true after the capture. (volume=0 keeps playback silent while the track stays live.)\n2) VIDEO LOAD NO TIMEOUT: in handleVideoFile, the `await new Promise((resolve,reject) => ... loadedmetadata ...)` has no timeout — a stalled file hangs forever after clearAll() already hid the empty state. Add a ~15s timeout that rejects with a clear error, and on failure call clearAll() or show the empty state again.\n3) SOURCE-CHIP FALSE ERROR: the source-chip drawImage (drawing the video poster) can throw InvalidStateError before a frame exists, rejecting handleVideoFile with a false "couldn\'t read" even though the loop is fine. Wrap that drawImage in try/catch (ignore errors) or only draw when readyState >= HAVE_CURRENT_DATA.\n4) GATE TEST HOOKS OUT OF PRODUCTION: the `window.__ramp / __lastChars / __selectedCps / __commonSet` assignments (in renderSource, applyCustomRamp, init) should only exist in dev. Wrap each in `if (import.meta.env.DEV) { ... }`. The tests run on the Vite dev server (DEV=true) so they still work; production ships no hooks.\n5) REPLAY BLOB LEAK: showReplay creates a new object URL per download and only revokes on Close. Track the previous URL and revoke it before assigning a new one.\n6) HUMAN COPY: the stat line currently reads "170 × 96 · 16320 letters · 130 distinct · Parchment". Humanize it but KEEP the word "letters" (tests assert it) and keep the palette name: e.g. "Your picture — 16,320 letters · Parchment". The status flash messages are fine.\n7) PRIVACY QUALIFICATION near Share: after a share/download, and in the share hint, make clear "the mosaic is shared — not your original photo — and nothing goes to geez·art\'s servers". Update the share-hint text and/or the doShare flash.\n8) ANNOUNCE ON READY: the status line is a role=status live region — when a mosaic finishes rendering, call flash("Your picture is ready!") so screen readers announce it. (Do this in renderSource, only on the first render or on each new source, not every slider tweak — e.g. only when fade is true.)\n9) PICKER ACCESSIBILITY: in buildPicker and updatePickerUI, add `aria-pressed` to the family tiles and individual letter buttons reflecting on/off state, and set `aria-label` on the zoom buttons is already done (index.html). For the picker letters, set aria-label to include the codepoint or a readable name (e.g. `Letter U+1200`) so screen readers can distinguish them.\n10) MOSAIC TEXT ALTERNATIVE: in renderSource, extend the canvas aria-label to include a SAMPLE of the actual fidel grid text (first ~200 chars of res.chars joined) so a screen reader gets real content, keeping the existing "Mosaic of Ethiopic letters" prefix (a test asserts it).\nVerify tsc.',
    { label: 'fix:app', phase: 'Fix', schema: RESULT }
  ),
  () => agent(
    PROJECT + '\n\nASSIGNED: index.html + src/style.css + create public/_headers (a Cloudflare Pages headers file). Do NOT touch app.ts.\nFixes:\n1) THREE-TAP DEFAULT (the biggest design complaint): wrap the Adjust group, the Letters group, and the extra export buttons (PNG/video/GIF/copy/HTML) inside a native `<details id="advanced"><summary>Advanced</summary> ... </details>` so a new user sees only: the drop target, Try an example, and Share (+ Download PNG stays visible). Use a native <details> — no JS needed. Put the Share button and Download PNG OUTSIDE the details (always visible).\n2) STICKY TOPBAR: make .topbar position:sticky; top:0; z-index:20; with a solid background so the Share button stays visible on mobile while scrolling.\n3) COPY: "Drop a photo or video" → "Choose a photo or video" (and the sub "tap to pick a photo or video" — drop the desktop "paste (Ctrl+V)" line or move it to the advanced hint). Update the empty state too.\n4) BILINGUAL AMHARIC: add an Amharic line to the key surfaces — empty state subtitle, the Share hint, and the footer — keeping the English (tests assert English substrings). E.g. empty-state h2: "Choose a photo or video" + <span lang="am">ምስል ወይም ቪዲዮ ይምረጡ</span>. Footer: add "ለፊደል በአክብሮት" alongside "Made with respect...". Keep it minimal and correct.\n5) HALO: make the halo STATIC — remove the haloBreathe animation (keep the soft gold glow, just don\'t pulse it).\n6) FOCUS/STATE CONTRAST: select:focus-visible outline and the gold state borders (.fam-tile.partial, .mix-btn, dropzone dragover) must be ≥3:1 — use the brick accent #a62b16 or a darker gold instead of #d9a441.\n7) ZOOM-BAR HIDDEN BUG: `.zoom-controls { display:flex }` overrides the [hidden] attribute. Add `.zoom-controls[hidden] { display:none; }` so the bar is truly hidden + out of the tab order until a mosaic exists.\n8) SKIP LINK: add a "Skip to artwork" link at the very top of <body> that jumps to #mosaic, styled to become visible on focus.\n9) OG IMAGE META: add `<meta property="og:image" content="/og-image.png">` and `<meta name="twitter:image" content="/og-image.png">` (the PNG is generated separately).\n10) CSP HEADERS: create public/_headers with: `/*` with `Content-Security-Policy: default-src \'self\'; img-src \'self\' data:; style-src \'self\' \'unsafe-inline\'; font-src \'self\' data:; connect-src \'self\'` plus `X-Content-Type-Options: nosniff` and `Referrer-Policy: strict-origin-when-cross-origin`.\nAlso add a small attribution line near the Palette selector: "Colors inspired by the manuscript and mural tradition of the Ethiopian Orthodox Church."\nVerify tsc (index.html/css don\'t type-check, but run it anyway to confirm nothing else broke).',
    { label: 'fix:html-css', phase: 'Fix', schema: RESULT }
  ),
  () => agent(
    PROJECT + '\n\nASSIGNED: create public/og-image.png (a 1200×630 branded card) and fix docs/architecture.md.\n1) Use Playwright (installed) to render a branded OG card in a real browser and save it as public/og-image.png:\n- Launch chromium, open a page, and in-page: draw a 1200×630 canvas with a parchment background, a small sample fidel mosaic (render ~24 fidel letters in gold/brick as a decorative band), the wordmark "ግዕዝ geez·art" large, and "Turn a photo into ፊደል letters — free, nothing uploaded". Use fonts that render Ethiopic (set the canvas font to a system Ethiopic font or load Noto via the app). Simpler: reuse the existing fontsource woff2 by importing it in the page or using the app\'s font. Get the PNG bytes and write public/og-image.png (create public/ dir if needed). Make it look clean and on-brand (parchment #f6f4f0, brick #a62b16, gold #d9a441, dark ink #1c1917).\n2) Fix the stale docs/architecture.md: change the "27 Playwright tests" references to the current 38 (and note the suite covers every input path, export, and the video/GIF/replay flows).\nVerify: public/og-image.png exists and is a valid PNG (check magic bytes).',
    { label: 'fix:og-image', phase: 'Fix', schema: RESULT }
  ),
])
const done = results.filter(Boolean)
log('Fix batch done: ' + done.length + '/6')
log(JSON.stringify(done.map((r) => ({ file: r.file, status: r.status })), null, 2))
return done